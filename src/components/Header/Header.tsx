import HeaderStyled from "./HeaderStyled";

const Header = (): React.ReactElement => {
  return (
    <HeaderStyled>
      <header className="web-title">Comerç Ideal</header>
    </HeaderStyled>
  );
};

export default Header;
